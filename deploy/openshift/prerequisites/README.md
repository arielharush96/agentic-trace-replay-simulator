# Cluster Prerequisites

These are reference materials for a cluster administrator. They are not part
of the namespaced benchmark base and must not be applied automatically by the
benchmark runner.

OpenShell, Agent Sandbox, required SCC permissions, TLS, image-pull Secrets,
and monitoring access must be reviewed and installed according to the target
cluster's security policy before running the benchmark.
